apiVersion: v1
kind: Service

metadata:
  name: nginx-service
  namespace: liora-dev


spec:
  type: NodePort

  selector:
    app: nginx-deployment

  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080