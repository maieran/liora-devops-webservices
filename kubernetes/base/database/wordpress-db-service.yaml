apiVersion: v1
kind: Service

metadata:
  name: wordpress-db
  namespace: liora-dev

spec:
  clusterIP: None

  selector:
    app: wordpress-db

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306