apiVersion: v1
kind: Service

metadata:
  name: prestashop-db-service-headless
  namespace: liora-dev

spec:
  clusterIP: None

  selector:
    app: prestashop-db

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306